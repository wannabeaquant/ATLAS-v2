[project]
name = "atlas-v2"
version = "0.1.0"
description = "Deterministic trading architecture scaffold inspired by ATLAS autoresearch."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
